# 傻瓜式使用指南 🚀

## 一、安装（1 分钟）

打开终端，进入项目目录，运行：

```bash
cd terminalCrypto


```

输入你的电脑密码，等待安装完成。

## 二、使用（超级简单）

### 方法 1：直接输入币种名

```bash
btc        # 查看 BTC 价格
eth        # 查看 ETH 价格
sol        # 查看 SOL 价格
```

### 方法 2：使用 price 命令

```bash
price btc              # 查看 BTC
price eth              # 查看 ETH
price btc eth sol      # 同时查看多个
```

**就这么简单！不需要任何配置！**

---

## 三、如果想要更多币种

### 添加你常用的币种（可选）

如果你想给其他币种也创建快捷命令，编辑一下安装脚本：

1. 打开 `super_simple_install.sh`
2. 找到创建命令的部分
3. 复制粘贴，改成你要的币种

例如添加 `doge` 命令：

```bash
cat > doge << 'EOF'
#!/bin/bash
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
exec "$SCRIPT_DIR/terminalcrypto" --exchange coinbase price DOGE
EOF
```

然后在安装部分添加：
```bash
sudo cp doge /usr/local/bin/
```

重新运行安装即可。

---

## 四、Coinbase API 密钥获取方法

### ⚠️ 重要提示
**大部分人不需要 API 密钥！** 默认就能查价格。

只有在以下情况才需要：
- 你每秒查询超过 10 次
- 你想要访问账户信息

### 如果真的需要，这样获取：

#### 第 1 步：注册 Coinbase 账号

访问：https://www.coinbase.com

#### 第 2 步：登录并进入 API 设置

1. 登录 Coinbase
2. 点击右上角头像
3. 选择 **Settings**（设置）
4. 左侧菜单选择 **API**

#### 第 3 步：创建 API Key

**注意：Coinbase 有两个版本的 API**

##### 选项 A：Coinbase Pro API（推荐，免费）

1. 访问：https://pro.coinbase.com
2. 登录后，点击右上角头像
3. 选择 **API**
4. 点击 **+ New API Key**
5. 权限设置：
   - ✅ 勾选 **View**（查看）
   - ❌ 不要勾选 **Trade**（交易）
   - ❌ 不要勾选 **Transfer**（转账）
6. 点击 **Create API Key**
7. 保存以下信息：
   - **API Key**（公钥）
   - **API Secret**（私钥）
   - **Passphrase**（密码短语）⚠️ 很重要！

##### 选项 B：Coinbase Advanced Trade API

1. 访问：https://www.coinbase.com/settings/api
2. 点击 **Create API Key**
3. 选择权限（只选查看）
4. 保存 API Key 和 Secret

#### 第 4 步：配置到工具

目前我们的工具**不需要配置 API**，公开访问就够了！

如果未来需要，会更新说明。

---

## 常见问题

### Q: 为什么我看到的价格和交易所不一样？

**A:** 我们默认使用 Coinbase（美元计价），和其他交易所可能有小幅差异。

### Q: 支持哪些币种？

**A:** 所有 Coinbase 支持的币种：
- BTC, ETH, SOL, ADA, DOT, LINK, MATIC, AVAX
- DOGE, SHIB, UNI, AAVE, ATOM, XRP, LTC
- 等等...

完整列表：https://www.coinbase.com/price

### Q: 可以切换到其他交易所吗？

**A:** 可以！但需要用稍微复杂一点的命令：

```bash
# 使用 Binance
terminalcrypto --exchange binance price BTC

# 使用 Coinbase
terminalcrypto --exchange coinbase price BTC
```

### Q: 出现错误怎么办？

**常见错误和解决方案：**

1. **"command not found: btc"**
   - 重新运行 `./super_simple_install.sh`

2. **"product not found"**
   - 这个币种 Coinbase 可能不支持
   - 试试其他主流币种

3. **网络错误**
   - 检查网络连接
   - 可能需要 VPN（某些地区）

---

## 总结

**安装：**
```bash
./super_simple_install.sh
```

**使用：**
```bash
btc         # 就这么简单！
eth
sol
```

或者：
```bash
price btc eth sol
```

**就这三个步骤，白痴都能用！** 🎉

---

## 附录：Coinbase API 链接快速导航

| 需要什么 | 链接 |
|---------|------|
| 注册账号 | https://www.coinbase.com |
| Coinbase Pro API | https://pro.coinbase.com |
| API 文档 | https://docs.cloud.coinbase.com |
| 支持的币种 | https://www.coinbase.com/price |

**再次强调：大部分人不需要 API 密钥！**

直接用就行了！🚀
